# .vimrc configuration
For now the setup is manual, but I would like to automate it at some point.

1) Place the .vimrc in the home directory
2) Make a ~/.vim/syntax directory if it doesn't exist and copy the glsl.vim file there
3) Use vim as usual.
